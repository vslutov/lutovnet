{
  "name": "lutov-net",
  "version": "0.1.2",
  "private": true,
  "dependencies": {
    "ramda": "^0.26.1",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "craco start",
    "build": "craco build",
    "test": "craco test",
    "clean": "rimraf build",
    "cz": "git-cz",
    "version": "npm install && npm run clean && npm run build && npm prune --production && npm dedupe && npm shrinkwrap && git add npm-shrinkwrap.json"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@craco/craco": "^3.6.0",
    "babel-plugin-transform-react-pug": "^7.0.0",
    "bootstrap": "^4.3.1",
    "coffee-loader": "^0.9.0",
    "coffeescript": "^2.3.2",
    "commitizen": "^3.0.7",
    "cz-conventional-changelog": "^2.1.0",
    "husky": "^2.1.0",
    "jest-preset-coffeescript": "^1.0.1",
    "node-sass": "^4.11.0",
    "pug-as-jsx-loader": "^1.0.66",
    "rimraf": "^2.6.3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "husky": {
    "hooks": {}
  }
}
